const nombre="paco";
const app= "la aplicacion";
let usoCompleto=`fase completa es ${nombre} y ${app}`;
console.log(usoCompleto);

let segundos = 60;
console.log(`El tiempo es ${segundos} segundos`);